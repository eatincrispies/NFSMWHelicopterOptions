// Validation.h - cross-field rules, filter-gain safety, dependency
// auto-enables and danger warnings. Per-key bounds live in the key table.
#pragma once

namespace Validation {

    void Sanitize();

} // namespace Validation
